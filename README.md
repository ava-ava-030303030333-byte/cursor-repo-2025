# Чатбот регистрации на мероприятие

Проект Python чатбота для регистрации участников на мероприятие.

## Функционал

- Собирание данных: ФИО, email, телефон, название организации
- Валидация email и телефона
- Сохранение регистраций в JSON файл
- Два модуля: консоль и Telegram бот

## Архитектура проекта

```
event_registration_bot/
├── config/
│   └── settings.py          # Настройки приложения
├── data/
│   └── registrations.json   # JSON файл с регистрациями
├── models/
│   └── registration.py      # Модель данных регистрации
├── validators/
│   └── validators.py       # Функции валидации
├── storage/
│   └── json_storage.py     # Сохранение в JSON
├── interfaces/
│   ├── console_interface.py # Консоль
│   └── telegram_bot.py     # Telegram бот
├── requirements.txt     # Зависимости
├── main.py              # Маин-скрипт
└── README.md
```

## Установка

```bash
pip install -r requirements.txt
```

## Запуск

На консоли:
```bash
python main.py
```

На Telegram (нужно указать токен в .env ныли в config/settings.py):
Выберите опцию 2 в меню

## Копирайт

Чатбот регистрации на мероприятие. 2025.

## Админ-меню

Для доступа к админ-меню:
- Выберите опцию 3 в главном меню
- Введите пароль: **1234**
- На консоли: команда 'admin' в меню
- На Telegram: команда '/admin'

В админ-меню можно:
- Посмотреть статистику (всего регистраций, размер БД)
- На консоли: 1 - все данные
- Цию: '0' для выхода

## Архитектура с Mermaid диаграммой

```mermaid
Вот исправленная диаграмма Mermaid с корректными русскими названиями и без опечаток:

```mermaid
%%{init: {'theme': 'default', 'themeVariables': { 'primaryColor': '#f0f8ff', 'edgeLabelBackground':'#fff', 'mainBkg': '#ffffff'}}}%%
flowchart TD
    subgraph "🖥️ Пользовательский интерфейс"
        direction TB
        U1[👤 Пользователь] -->|1. Ввод данных| C1[Консольный интерфейс\n(console_interface.py)]
        U1 -->|2. Telegram-чат| T1[Telegram-бот\n(telegram_bot.py)]

        A1[👨‍💼 Администратор] -->|1. Команда 'admin' в консоли| AC[Админ-панель (консоль)\n(admin_console.py)]
        A1 -->|2. Команда /admin в Telegram| AT[Админ-панель (Telegram)\n(admin_telegram.py)]
    end

    subgraph "⚙️ Слой диалога и управления состоянием"
        direction LR
        C1 --> FSM1[Состояния регистрации\n(консоль)]
        T1 --> FSM2[Состояния регистрации\n(aiogram FSM)]

        AC --> ADM_C[Меню администратора\n(консоль)]
        AT --> ADM_T[Меню администратора\n(Telegram States)]
    end

    subgraph "📦 Бизнес-логика"
        direction LR

        subgraph "✅ Валидация данных"
            V1[Валидация ФИО]\n"validators/validators.py"
            V2[Валидация email]
            V3[Валидация телефона]
            V4[Валидация организации]
        end

        subgraph "📊 Модель данных"
            M1[Класс Registration]\n"dataclass\nmodels/registration.py"
        end

        subgraph "💾 Работа с хранилищем"
            S1[Загрузка регистраций]\n"storage/json_storage.py"
            S2[Сохранение регистраций]
        end

        FSM1 & FSM2 --> V1 & V2 & V3 & V4
        V1 & V2 & V3 & V4 --> M1
        M1 --> S2
        ADM_C & ADM_T --> S1
    end

    subgraph "🔐 Конфигурация и безопасность"
        CONF[config/settings.py\n• Путь к файлу данных\n• Токен Telegram-бота\n• Пароль администратора]
        CONF --> C1 & T1 & AC & AT
    end

    subgraph "🗄️ Хранилище данных"
        JSON_FILE["data/registrations.json\n(UTF-8, формат JSON)"]
        S1 & S2 <--> JSON_FILE
    end

    subgraph "🎛️ Главный модуль"
        MAIN[main.py\n• Меню выбора режима\n• Запуск интерфейсов\n• Завершение работы]
        MAIN --> C1 & T1 & AC
    end

    %% Связи между уровнями
    MAIN -.->|запускает| "🖥️ Пользовательский интерфейс"
    "🖥️ Пользовательский интерфейс" -->|управляет| "⚙️ Слой диалога и управления состоянием"
    "⚙️ Слой диалога и управления состоянием" -->|вызывает| "📦 Бизнес-логика"
    "📦 Бизнес-логика" -->|читает и записывает| "🗄️ Хранилище данных"
    CONF -->|предоставляет параметры| "⚙️ Слой диалога и управления состоянием"
    CONF -->|указывает путь| "🗄️ Хранилище данных"

    %% Цветовые стили
    classDef user fill:#e6f7ff,stroke:#1890ff;
    classDef app fill:#f6ffed,stroke:#52c41a;
    classDef logic fill:#fff7e6,stroke:#fa8c16;
    classDef storage fill:#f9f0ff,stroke:#722ed1;
    classDef config fill:#fff1f0,stroke:#f5222d;
    classDef main fill:#fafafa,stroke:#8c8c8c,stroke-width:2px;

    class U1,A1 user
    class C1,T1,AC,AT,FSM1,FSM2,ADM_C,ADM_T,MAIN app
    class V1,V2,V3,V4,M1 logic
    class S1,S2,JSON_FILE storage
    class CONF config
```
